module.exports = {<% if (isTypeScriptProject) { %>
  parser: "@typescript-eslint/parser" %>",
  plugins: [ "@typescript-eslint" ],<% } %>
  extends: [ "@landonschropp"<%- isReactProject ? ', "@landonschropp/react"' : '' %><%- isTypeScriptProject ? ', "plugin:@typescript-eslint/recommended"' : '' %> ],<% if (!isTypeScriptProject) { %>
  parserOptions: {
    sourceType: "module",
    ecmaVersion: "latest"
  },<% } %>
  env: {
    es6: true,
    <%= isReactProject ? 'browser' : 'node' %>: true<% if (isJestProject) { %>,
    jest: true<% } %>
  }
};
